Angular UI (placeholder)

This directory is a placeholder for the Angular + PrimeNG application.

Expected routes
- /home, /programs, /store, /settings, /logs, /plugin/:id

Build output should be placed under dist/ and copied to:
- ../core-service/src/main/resources/static/

